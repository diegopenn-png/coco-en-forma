import fs from 'node:fs';
const path='eterna-worker/src/index.js';
let s=fs.readFileSync(path,'utf8');
s=s.replace('const VERSION="160.99.15-general-worksheet-vision";','const VERSION="160.99.16-current-turn-subject-vision";');
const director='function explicitAcademicSwitch(text){const n=directorNorm(text);return /^(?:(?:vale|ok) )?(?:ahora|cambiando de tema|cambio de tema|volvamos|retomemos|sigamos con|continuemos con)\\b/.test(n)||/\\b(?:matematicas|lengua|ingles|biologia|fisica|quimica|historia|geografia|examen|deberes|tarea|ejercicio)\\b/.test(n)}';
if(!s.includes(director)) throw new Error('director anchor missing');
const helper=`\nfunction currentTurnSubjectHint(text){const n=directorNorm(text);const rules=[['Matemáticas',/\\b(?:matematicas|mates|fraccion|division|multiplicacion|suma|resta|ecuacion|numero)\\b/],['Lengua',/\\b(?:lengua|ortografia|gramatica|prefijo|sufijo|antonimo|sinonimo|verbo|sustantivo|adjetivo|completa con b|completa con v)\\b/],['Inglés',/\\b(?:ingles|english|vocabulary|grammar)\\b/],['Biología',/\\b(?:biologia|celula|ecosistema|seres vivos)\\b/],['Física',/\\b(?:fisica|velocidad|fuerza|energia)\\b/],['Química',/\\b(?:quimica|atomo|molecula|elemento quimico)\\b/],['Historia',/\\b(?:historia|edad media|revolucion|imperio)\\b/],['Geografía',/\\b(?:geografia|continente|relieve|rio|capital)\\b/]];for(const [subject,re] of rules)if(re.test(n))return subject;return null}\nfunction currentTurnPriorityInstruction(text,image){const subject=currentTurnSubjectHint(text);return image?\`PRIORIDAD DEL TURNO ACTUAL: la imagen adjunta y el mensaje actual mandan sobre historial, materia, ejercicio y pregunta pendientes anteriores. ${'${subject?`El alumno identifica o sugiere la materia actual como '+subject+'. ':\'\'}'}Determina la materia de ESTA imagen desde su contenido. Si contradice el hilo anterior, cambia de materia y suspende el hilo previo. Nunca describas letras, palabras o huecos ortográficos como números. No reutilices una aclaración de baja confianza propia de otra asignatura.\`:subject?\`PRIORIDAD DEL TURNO ACTUAL: el mensaje actual indica ${'${subject}'}; si contradice la materia anterior, cambia de materia y suspende el hilo previo.\`:''}\n`;
s=s.replace(director,director+helper);
const marker='MENSAJE ACTUAL PRIORITARIO=${JSON.stringify(String(text||"").slice(0,5000))}.';
if(!s.includes(marker)) throw new Error('tutor priority anchor missing');
s=s.replace(marker,'${currentTurnPriorityInstruction(text,image)}\\n'+marker);
const visionMarker='Esta es la zona ${index+1} de ${generalImages.length} de la misma hoja. Es una lectura GENERAL de material escolar:';
if(s.includes(visionMarker)) s=s.replace(visionMarker,'PRIORIDAD: interpreta esta zona como parte de la imagen del TURNO ACTUAL; ignora la materia histórica si contradice lo visible o el mensaje actual. Esta es la zona ${index+1} de ${generalImages.length} de la misma hoja. Es una lectura GENERAL de material escolar:');
fs.writeFileSync(path,s);
