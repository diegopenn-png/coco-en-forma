{
  "name": "coco-eterna-worker-v159",
  "private": true,
  "version": "160.91.2",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "upload:preview": "wrangler versions upload",
    "rollback:production": "wrangler rollback",
    "test": "node --test test/*.test.mjs",
    "check": "node --check src/index.js && node --test test/*.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
